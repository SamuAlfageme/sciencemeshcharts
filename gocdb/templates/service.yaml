apiVersion: v1
kind: Service
metadata:
  name: {{ include "gocdb.fullname" . }}
  labels:
    {{- include "gocdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gocdb.selectorLabels" . | nindent 4 }}
